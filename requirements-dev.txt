# Development dependencies — the test suite and the linter.
# Runtime deps are in requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Note that the *suite* still needs PyQt5 even though almost none of the code
# under test does. Nearly every module is Qt-free by design (see CLAUDE.md), but
# tests/test_ask_lifecycle.py and tests/test_search_worker.py import PyQt5 at
# module scope to drive the worker threads, so collection fails without it.
# Running those headless needs QT_QPA_PLATFORM=offscreen (see .github/workflows).

# ruff is pinned exactly, not floored. Its `F` rule family gains checks between
# minor releases, so an unpinned floor lets a ruff release turn master red on a
# day nobody touched the repo. Bump it deliberately, as a reviewed change.
ruff==0.15.22

# Capped at the major: pytest's breaking changes land there, and the suite has
# no reason to opt into pytest 10 the day it ships.
pytest>=8.0,<10

coverage>=7.0
