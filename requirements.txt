# D&D 2e Rules Desktop App – dependencies
# Install with:  pip install -r requirements.txt

# Core Qt UI
PyQt5>=5.15.0

# HTML content renderer (strongly recommended – falls back to plain text if missing)
PyQtWebEngine>=5.15.0

# "Ask the Rules" runs a LOCAL model via Ollama (https://ollama.com) — no Python
# package needed (the app talks to Ollama's HTTP API with the standard library).
# Install Ollama separately and pull a model, e.g.:  ollama pull llama3.1

# Scraper dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0

# Optional: only needed if regalgoblins.com blocks direct requests
# Install with:  pip install playwright && playwright install chromium
# playwright>=1.30.0
